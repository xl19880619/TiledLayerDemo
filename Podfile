# Uncomment the next line to define a global platform for your project
 platform :ios, '10.0'

target 'TiledLayerDemo' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for TiledLayerDemo
  pod 'SDWebImage'
  pod 'Masonry'
  pod 'MBProgressHUD'

  target 'TiledLayerDemoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TiledLayerDemoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
